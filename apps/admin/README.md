# Admin Worker

The Admin app is a Spanish first Foundation shell with no privileged bootstrap users or business features. Browser API calls use relative `/api/*` paths and the `API_SERVICE` service binding. The Worker has no D1, R2, or authentication secrets.
